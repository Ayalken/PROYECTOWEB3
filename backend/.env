# Archivo .env deshabilitado por petición del usuario
# El proyecto funciona sin archivo .env — las claves y configuraciones
# por defecto están definidas en el código (no recomendado en producción).
# Si quieres eliminar este archivo por completo, renómbralo o bórralo manualmente.

# Nota: Para producción, mueve las claves sensibles a un gestor de secretos.

# Entorno
NODE_ENV=development
