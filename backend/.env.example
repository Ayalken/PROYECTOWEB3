# Variables de Entorno
# Copia este archivo a .env y reemplaza los valores con los reales

# JWT Secret (Clave secreta para firmar tokens JWT)
JWT_SECRET=tu_clave_secreta_super_segura_aqui_cambiar

# Google reCAPTCHA v3 (Obtén las claves en https://www.google.com/recaptcha/admin)
RECAPTCHA_SECRET=tu_secret_key_de_recaptcha_aqui

# Base de Datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=registro_notas

# Puerto del Servidor
PORT=3000
